# 엔카북카(Encar Bookcar) 개발 로드맵

## 1. 제품 목표

엔카북카는 회사 동료가 보유한 책을 등록하고 발견하며, 서로 안전하게 빌려 읽을 수 있도록 돕는 사내 북클럽 애플리케이션이다.

초기 목표는 다음 흐름을 완성하는 것이다.

1. 회사 구성원이 가입하고 로그인한다.
2. 자신이 보유한 책을 등록한다.
3. 다른 동료가 등록한 책을 검색하고 상세 정보를 확인한다.
4. 책을 빌리고 반납할 수 있다.

## 2. 개발 원칙

- MVP에서는 핵심 독서 공유 흐름에 집중한다.
- 한 화면에서는 하나의 주요 행동을 명확하게 보여준다.
- 서버 컴포넌트를 기본으로 사용하고, 상호작용이 필요한 부분만 클라이언트 컴포넌트로 구현한다.
- 입력값은 클라이언트와 서버에서 모두 Zod로 검증한다.
- 인증과 권한 검사는 화면뿐 아니라 서버에서도 수행한다.
- 데이터베이스 변경은 Drizzle 스키마와 마이그레이션으로 관리한다.
- 각 Phase는 린트, 타입 검사, 테스트, 프로덕션 빌드를 통과해야 완료된다.

## Phase 0. 프로젝트 기반과 홈 화면 — 완료

### 목표

제품의 디자인 언어와 기술적 기반을 만들고 방문자가 서비스 목적을 이해할 수 있는 홈 화면을 제공한다.

### 완료된 기능

- Next.js App Router와 TypeScript 프로젝트 구성
- Tailwind CSS 기반 디자인 토큰과 반응형 레이아웃
- 엔카북카 홈 화면
- 도서 검색 인터랙션 데모
- TanStack Query Provider 구성
- PostgreSQL, Drizzle ORM, Better Auth, Zod 기반 패키지 설치
- 기본 도서 스키마와 환경 변수 예시
- Vitest와 Testing Library 테스트 환경
- 엔카북카 디자인 스킬 등록

### Phase 0 종료 시 제한 사항

- 홈 화면 도서는 샘플 데이터다.
- 당시 로그인과 책 등록 버튼의 실제 페이지는 없었다.
- 당시 데이터베이스와 인증 서비스는 연결되지 않았다.

## Phase 1. 회원가입과 로그인 — 완료

### 목표

회사 구성원이 안전하게 계정을 만들고 로그인 상태를 유지할 수 있게 한다.

### 주요 기능

- 이메일과 비밀번호 회원가입
- 로그인과 로그아웃
- 비밀번호 확인 및 안전한 비밀번호 규칙
- 회사 이메일 도메인 제한 또는 초대코드 정책
- 세션 생성, 갱신, 만료 처리
- 로그인 사용자 프로필 표시
- 비로그인 사용자의 보호 페이지 접근 차단
- 로그인 후 원래 접근하려던 페이지로 이동

### 화면

- `/signup`: 회원가입
- `/login`: 로그인
- `/profile`: 기본 프로필

### 데이터 모델

- `users`
- `sessions`
- `accounts`
- `verifications`
- 필요 시 `invitations`

### 서버 작업

- Better Auth 설정
- Drizzle용 인증 테이블 생성
- 인증 Route Handler 연결
- 서버 컴포넌트와 서버 액션에서 세션 검증
- 보호 경로용 Proxy 구성
- 회원가입 입력 Zod 스키마 작성

### 테스트

- 올바른 정보로 회원가입할 수 있다.
- 중복 이메일 가입을 차단한다.
- 잘못된 로그인 정보를 안내한다.
- 로그인 후 세션이 유지된다.
- 비로그인 사용자는 보호 페이지에 접근할 수 없다.
- 로그아웃 후 보호 페이지 접근이 차단된다.

### 완료 조건

- 실제 데이터베이스에서 회원과 세션이 생성된다.
- 새 사용자가 가입, 로그인, 로그아웃을 완료할 수 있다.
- 인증 오류가 쉬운 한국어 문구로 표시된다.
- 인증 정보가 클라이언트에 불필요하게 노출되지 않는다.

### 완료된 구현

- Better Auth 이메일·비밀번호 회원가입, 로그인, 로그아웃
- PGlite 기반 로컬 PostgreSQL 호환 저장소와 Drizzle 자동 마이그레이션
- 사용자, 세션, 계정, 인증 및 책 소유자 외래키 스키마
- 영문·숫자를 포함한 8자 이상 비밀번호 정책의 클라이언트·서버 검증
- 환경 변수 기반 회사 이메일 도메인 제한
- 7일 세션, 1일 단위 세션 갱신, 인증 엔드포인트 요청 제한
- `/profile` 및 `/books/new`의 Proxy 사전 차단과 서버 세션 재검증
- 로그인 후 안전한 내부 경로로 복귀
- 토스풍 반응형 회원가입, 로그인, 프로필 화면

### 검증 결과

- 정상 회원가입, 세션 조회, 로그아웃, 재로그인 성공
- 중복 이메일 가입과 잘못된 비밀번호 로그인 차단
- 로그아웃 후 보호 페이지 접근 시 로그인 화면으로 이동
- 타입 검사, ESLint, Vitest 7개 테스트, Next.js 프로덕션 빌드 통과

## Phase 2. 책 등록과 도서 탐색 — 완료, MVP 완성

### 목표

회원이 책을 등록하고 동료가 등록한 책을 검색하거나 상세 정보로 확인할 수 있게 한다.

### 주요 기능

- 책 등록
- 책 목록 조회
- 책 제목과 저자 검색
- 대여 가능 여부 필터
- 최신 등록순 정렬
- 책 상세 조회
- 자신이 등록한 책 수정과 삭제
- 등록자 정보와 등록일 표시
- 빈 목록, 로딩, 오류 상태

### 화면

- `/books`: 전체 책 목록
- `/books/new`: 책 등록
- `/books/[bookId]`: 책 상세
- `/books/[bookId]/edit`: 책 수정
- 홈 화면의 샘플 도서를 실제 데이터로 교체

### 책 등록 필드

- 제목
- 저자
- 표지 이미지 URL 또는 이미지 업로드
- 간단한 소개
- 책 상태
- 보관 장소 또는 전달 방법
- 등록자

### 데이터 모델

- `books`
- 필요 시 `book_images`
- 향후 확장을 위한 `book_status` 값
  - `AVAILABLE`
  - `BORROWED`
  - `UNAVAILABLE`

### 서버 작업

- 책 CRUD Route Handler 또는 서버 액션
- 소유자 기반 수정·삭제 권한 검사
- 검색과 페이지네이션 쿼리
- React Query mutation 후 관련 목록 캐시 갱신
- 서버와 클라이언트에서 동일한 Zod 검증 규칙 사용
- 책 삭제 시 연결 데이터 처리 정책 적용

### 테스트

- 로그인한 회원이 책을 등록할 수 있다.
- 필수값이 없으면 등록되지 않는다.
- 제목과 저자로 검색할 수 있다.
- 책 상세 정보가 정확히 표시된다.
- 등록자만 자신의 책을 수정하거나 삭제할 수 있다.
- 존재하지 않는 책은 404 화면으로 처리된다.

### 완료 조건

- 회원가입부터 책 등록과 상세 조회까지 실제 데이터로 동작한다.
- 홈 화면과 책 목록이 데이터베이스의 책을 표시한다.
- 모바일과 데스크톱에서 모든 핵심 작업을 수행할 수 있다.
- 이 Phase 완료 시 최초 사내 사용자 테스트가 가능한 MVP가 된다.

### 완료된 구현

- 카카오 책 검색 API 서버 프록시와 비밀 키 보호
- 검색 결과의 표지, 제목, 저자, 출판사, ISBN, 소개, 출간일 자동 입력
- 도서 등록·목록·상세·수정·삭제 Route Handler
- 제목·저자 검색, 대여 상태 필터, 등록일 정렬, 페이지네이션
- 등록자 전용 수정·삭제 서버 권한 검사
- 대여 상태, 보관 장소, 카카오 도서 링크 표시
- React Query 기반 로딩·오류·빈 결과·캐시 갱신
- 홈 화면 샘플 데이터를 실제 등록 도서로 교체
- 도서 테이블 제약조건과 검색·정렬용 인덱스 추가

### 검증 결과

- 도서 생성·검색·상세·수정·삭제 성공
- 다른 사용자의 수정 요청 403 차단
- 삭제한 도서의 상세 요청 404 처리
- 카카오 API 키 미설정 상태의 안내 응답 확인

## Phase 3. 대여와 반납

### 목표

책의 실제 대여 상태를 관리하고 소유자와 대여자 모두 현재 상태를 쉽게 확인할 수 있게 한다.

### 주요 기능

- 대여 요청
- 소유자의 요청 승인과 거절
- 대여 중 상태 표시
- 반납 요청과 반납 완료
- 내가 빌린 책과 빌려준 책 목록
- 중복 대여 방지
- 요청 취소
- 대여 이력

### 화면

- `/loans`: 내 대여 현황
- `/loans/requests`: 받은 대여 요청
- 책 상세 화면의 대여 요청 영역

### 데이터 모델

- `loans`
- 대여 상태
  - `REQUESTED`
  - `APPROVED`
  - `REJECTED`
  - `BORROWED`
  - `RETURN_REQUESTED`
  - `RETURNED`
  - `CANCELLED`

### 핵심 규칙

- 자신의 책은 빌릴 수 없다.
- 대여 가능한 책만 요청할 수 있다.
- 한 책에는 동시에 하나의 활성 대여만 존재할 수 있다.
- 승인과 반납 완료는 책 소유자만 처리할 수 있다.
- 상태 변경은 트랜잭션으로 처리한다.

### 테스트

- 동시 대여 요청에서도 하나의 요청만 승인된다.
- 권한이 없는 사용자는 대여 상태를 변경할 수 없다.
- 반납 완료 후 책이 다시 대여 가능 상태가 된다.
- 대여 이력이 삭제되지 않고 보존된다.

### 완료 조건

- 요청부터 승인, 대여, 반납까지 전체 흐름이 동작한다.
- 잘못된 상태 전환과 중복 대여가 서버에서 차단된다.

## Phase 4. 운영 기능과 출시 준비

### 목표

동호회 운영자가 서비스를 관리하고 사내에서 안정적으로 사용할 수 있는 상태를 만든다.

### 주요 기능

- 관리자와 일반회원 역할 구분
- 회원 목록과 상태 관리
- 부적절하거나 중복된 책 숨김 처리
- 기본 운영 통계
- 이메일 인증과 비밀번호 재설정
- 데이터베이스 백업 정책
- 오류 모니터링과 구조화된 로그
- 접근성 및 주요 브라우저 점검
- 개인정보 처리와 이용 안내
- 운영 환경 배포

### 관리자 화면

- `/admin`
- `/admin/members`
- `/admin/books`

### 품질 작업

- Playwright 핵심 사용자 흐름 테스트
- 로딩 속도와 이미지 최적화
- 키보드 탐색과 스크린 리더 확인
- 보안 헤더와 요청 제한
- 인증 및 권한 우회 테스트
- 데이터베이스 인덱스와 느린 쿼리 점검

### 완료 조건

- 운영자가 코드 수정 없이 기본 회원과 책을 관리할 수 있다.
- 주요 사용자 흐름의 E2E 테스트가 통과한다.
- 배포 환경에서 오류 추적과 복구 절차가 준비된다.

## Phase 5. 커뮤니티 확장

### 목표

기본 서비스 사용 데이터를 확인한 후 독서 모임의 참여와 발견 경험을 강화한다.

### 후보 기능

- 한 줄 추천과 독후감
- 별점 또는 공감
- 댓글
- 읽고 싶은 책 저장
- 인기 도서와 추천 도서
- 독서 모임 일정
- 월간 독서 통계
- 신규 책과 대여 요청 알림
- Slack 또는 이메일 알림 연동
- ISBN 검색을 통한 도서 정보 자동 입력

### 진행 원칙

- 실제 사용자 요청과 사용 빈도를 확인한 기능부터 개발한다.
- Phase 1~4의 핵심 흐름을 복잡하게 만드는 기능은 추가하지 않는다.

## 3. 권장 구현 순서

1. PostgreSQL 개발 데이터베이스 연결
2. Better Auth 테이블과 인증 설정
3. 회원가입과 로그인 화면
4. 보호 경로와 세션 검증
5. 책 스키마 확정 및 마이그레이션
6. 책 등록
7. 책 목록과 검색
8. 책 상세, 수정, 삭제
9. 홈 화면을 실제 도서 데이터로 전환
10. MVP 통합 테스트
11. 대여와 반납
12. 관리자와 출시 준비

## 4. 공통 완료 기준

각 Phase는 다음 기준을 모두 만족해야 완료로 처리한다.

- 요구된 사용자 흐름이 실제 데이터로 동작한다.
- 서버 인증과 권한 검사가 적용된다.
- 입력값을 Zod로 검증한다.
- 로딩, 빈 데이터, 오류 상태가 구현된다.
- 모바일과 데스크톱 레이아웃을 확인한다.
- 접근 가능한 라벨과 키보드 포커스를 제공한다.
- `npm run lint`가 통과한다.
- `npm run typecheck`가 통과한다.
- 관련 단위 및 통합 테스트가 통과한다.
- `npm run build`가 통과한다.

## 5. MVP 범위 요약

MVP는 Phase 1과 Phase 2까지다.

MVP에 포함되는 기능:

- 회원가입
- 로그인과 로그아웃
- 책 등록
- 책 목록과 검색
- 책 상세
- 자신의 책 수정과 삭제

MVP에서 제외되는 기능:

- 대여와 반납
- 댓글과 독후감
- 별점과 추천 알고리즘
- 모임 일정
- 외부 알림 연동
- 고급 관리자 통계

대여 기능은 MVP 사용성 확인 후 Phase 3에서 추가한다.
